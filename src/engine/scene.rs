//====================================================================

use crate::engine::StateInner;

//====================================================================

pub trait Scene {
    fn new() -> Self
    where
        Self: Sized;

    fn tick(&mut self, state: &mut StateInner);
}

//====================================================================
